export interface Aluno {
    aluno: {
        active: boolean,
        aluno: boolean,
        email: string,
        id: string,
        nome: string
        matricula: number,
        senha: string
    };
}